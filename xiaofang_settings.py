# -*- coding: utf-8 -*-
# ============================================================
# AI 小方 · 设置持久化文件 (V0.9 Alpha)
# ------------------------------------------------------------
# 本文件是"源码级永久保存"的设置 —— 你在聊天框输入 `setting`
# 进入设置后做出的修改，会直接改写下面这些常量并写盘保存，
# 下次启动自动生效。请不要手动删除或改乱结构。
# ============================================================

# 思考模式 ----------------------------------------------------
#   off   = 不思考：不做深度思考，直接生成回答（最快）
#   think = 单轮思考：每次都先深度思考再回答（默认，最稳）
#   multi = 多轮思考：小方边答边想、边想边答（类 DeepSeek V4 Pro 风格）
THINK_MODE = 'think'

# 多轮思考的轮数（THINK_MODE=multi 时生效，越大想得越细但越慢）
THINK_TURNS = 3

# 情绪敏感度（放大/减弱情绪权重的系数，1.0=标准，越大对小方情绪越敏感）
EMOTION_SENSITIVITY = 1.2

# 情绪识别是否纳入标点符号（！！=激动、？？=困惑、…=欲言又止）与 emoji（😊=开心、😭=难过）
USE_PUNCT_EMOJI = True

# 深度思考是否打印打字机效果（逐行打字、看起来像真在"想"）
SHOW_DEEP_THINK = True

# 回答的活泼程度（越大 emoji/语气越丰富）
ENERGY = 0.9

# 禁止联网搜索（True 后即使遇到不会的问题也不上网，仅本地回答）
FORCE_OFFLINE = False

# 速度档位（v1.7 Flash 新增）-------------------------------------
#   0 = 原速   ：打字机/思考段间隔与旧版一致（慢，但每行都看得清）
#   1 = 轻快   ：延迟约压缩 1/3
#   2 = Flash  ：默认档。只压"显示与采样预算"，不动模型与知识库 → 正确率不变、体感明显更快
#   3 = 极速   ：几乎瞬出（打字机延迟归零），适合批量跑测试/长回答
#   改法：输入 `setting` 后选，或直接改这个数字，也可 `setting 速度 3`
FLASH_LEVEL = 2