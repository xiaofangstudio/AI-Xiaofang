# Python 缓存
__pycache__/
*.pyc

# 首次启动自检留下的环境记号（本机生成，不该进仓库）
.xf_env_ok

# 训练出来的权重（几十 MB 的二进制，本机产物，不进仓库）
*.npz
*.npz.bak*
*.npz.good_*
*.npz.corrupt_*

# 冻结权重的磁盘缓存（2.3GB，本机生成，绝不进仓库）
权重缓存/

# 运行时数据
Web对话/
Web界面/_port.txt
背景图/
xiaofang_memory.json

# 编辑器 / 工具产物
.idea/
.vscode/
.trae-html-share-packages/
