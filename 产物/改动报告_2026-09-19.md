# Covi 1 显示名统一 + 「生成画面」→「产物」改名 · 改动明细

- 改动日期：2026-09-19
- 改动方式：全部在原有文件上原地修改，未新建任何带版本号的文件
- 变更文件数：3（xiaofang_web.py、svgdream_covi1.py、README.md）+ 磁盘目录改名 1 处

---

## 一、用户可见文案：「Covi 一」→「Covi 1」

文件：`xiaofang_web.py`

| 行号 | 改动前 | 改动后 |
| --- | --- | --- |
| 442 | `id="covi1_" + t["key"], label="Covi 一 " + t["name"],` | `id="covi1_" + t["key"], label="Covi 1 " + t["name"],` |
| 447 | `official.append(dict(id="covi1", label="Covi 一", desc="现役正式版 · 三档可选",` | `official.append(dict(id="covi1", label="Covi 1", desc="现役正式版 · 三档可选",` |
| 455 | `dict(desc="已并入 Covi 一 · 仅作存档", badge="备案")))` | `dict(desc="已并入 Covi 1 · 仅作存档", badge="备案")))` |
| 1367 | `label = a.label or ("Covi 一 " + a.tier.capitalize() if not a.legacy` | `label = a.label or ("Covi 1 " + a.tier.capitalize() if not a.legacy` |

说明：这 4 处分别对应「模型树里 Lite / Pro / Ultra 三档的显示名」「正式版分组名」「Covi2 合并备案项的说明文案」「CLI / 启动参数默认 label（即窗口标题与页面标题里显示的模型名）」，都是会直接渲染给用户看的字符串。内部标识符 `id="covi1"`、`STATE["model_id"]="covi1_<tier>"`、环境变量名等**未改动**，避免影响前后端按 id 匹配的逻辑。

保留项（未改，待你确认）：`xiaofang_web.py` L453 的 `_mk_item("Covi 二（合并备案）", ...)` 里是中文「二」作版本号。本次指令只要求把中文「一」换成阿拉伯 1，故这一处按原样保留；若也要统一，可一并改为「Covi 2（合并备案）」。

---

## 二、「生成画面」→「产物」

### 2.1 代码内路径常量（文件：`svgdream_covi1.py`）

| 行号 | 所在函数 / 常量 | 改动前 | 改动后 |
| --- | --- | --- | --- |
| 619 | `pelican_bike_save()` SVG 落盘目录 | `base = folder or os.path.join(os.path.dirname(os.path.abspath(__file__)), "生成画面")` | `... , "产物")` |
| 806 | `pelican_save_prompt()` 真图 Prompt 落盘目录 | `base = folder or os.path.join(os.path.dirname(os.path.abspath(__file__)), "生成画面")` | `... , "产物")` |
| 917 | 模块级常量 `PELICAN_SVG_DIR` | `PELICAN_SVG_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "生成画面")` | `PELICAN_SVG_DIR = os.path.join(os.path.dirname(os.path.abspath(__file__)), "产物")` |

连带生效点（无需单独改动，均引用常量）：`svgdream_covi1.py` L933 `SVGDREAM_SVG_DIR = PELICAN_SVG_DIR`、L1839 `svgdream_save()`、L1853 `svgdream_save_prompt()`、L1901 `svgdream_answer()`；以及 `xiaofang_covi1.py` L1049 `svgdream_svg_dir = _SVGD.SVGDREAM_SVG_DIR`。

### 2.2 README 文案（文件：`README.md`）

| 行号 | 改动前 | 改动后 |
| --- | --- | --- |
| 250 | `├── 生成画面/                      # 独立 SVG 引擎(svgdream_covi1.py)产出的动画` | `├── 产物/                          # 独立 SVG 引擎(svgdream_covi1.py)产出的动画` |

### 2.3 磁盘目录实际改名

- 由：`c:\Users\ASUS\Desktop\AI 小方\生成画面\`（空目录）
- 改为：`c:\Users\ASUS\Desktop\AI 小方\产物\`
- 命令：`Rename-Item -LiteralPath '<项目>\生成画面' -NewName '产物'`，目录原地改名，未产生新目录副本，原时间戳保留（2026/9/13 23:33:26）。

### 2.4 未改动（有意保留）

`历史版本\_训练脚本存档\优质语料\语料_001.txt`、`语料_003.txt`、`语料_007.txt` 中出现的「生成画面」「画面」是**训练语料正文内容**，与目录名无关，改动会污染语料，故保持原样。除此之外全项目已无「生成画面」残留（含 .bat / .ps1 / .js / .html / .css / .json / .md 全量搜索）。

---

## 三、验证结果

### 3.1 语法校验（`python -m py_compile`）

```
python -m py_compile "c:\Users\ASUS\Desktop\AI 小方\xiaofang_web.py" "c:\Users\ASUS\Desktop\AI 小方\svgdream_covi1.py"
py_compile_exit=0
```

结果：两个改动过的 Python 文件均**编译通过，退出码 0**，无 SyntaxError / IndentationError。

### 3.2 运行时路径与文案断言

导入 `svgdream_covi1`（纯标准库，不加载权重）后实测：

```
SVGDREAM_SVG_DIR = c:\Users\ASUS\Desktop\AI 小方\产物
PELICAN_SVG_DIR  = c:\Users\ASUS\Desktop\AI 小方\产物
dir_exists       = True
dir_name         = 产物
label_covi1_title  = True      # label="Covi 1"
label_covi1_tier   = True      # label="Covi 1 " + t["name"]
label_cli_prefix   = True      # ("Covi 1 " + a.tier.capitalize()
desc_merged        = True      # 已并入 Covi 1
残留_Covi_一        = False
```

### 3.3 全项目残留搜索

- `Covi 一`：0 命中（排除 `历史版本/` 归档语料与历史归档源码）
- `生成画面`：0 命中（排除 `历史版本/_训练脚本存档/优质语料/` 正文）

---

## 四、交付说明

- 本次改动无「较长新代码」产出，全部为原地修改；本文档即按约定写入产物文件夹的长内容。
- 产物文件夹：`c:\Users\ASUS\Desktop\AI 小方\产物\`
- 本文档：`c:\Users\ASUS\Desktop\AI 小方\产物\改动报告_2026-09-19.md`
- 此后 svgdream 引擎生成的 `.svg` 与 `.prompt.txt` 会直接落在该「产物」文件夹下。
