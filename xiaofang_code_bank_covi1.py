# -*- coding: utf-8 -*-
# ════════════════════════════════════════════════════════════════════════════
# xiaofang_code_bank_covi1.py  ·  Data 代码区
#   Covi 1 的「代码知识库」—— 教小方【懂】代码, 不是【背】代码。
#   每一类是什么、怎么写、怎么用、底层原理, 提炼成可读的中文教学条目。
#   来源两条腿:
#     ① 下面手写的 Python 核心知识条目(语言本质/语法/数据结构/进阶/算法原理/生态);
#     ② 训练启动时自动扫描自身与历史版本的 .py, 把「函数签名 + docstring 首句」
#        提炼成"这个函数是干什么的、怎么用"的条目(见 covi1 的 _auto_code_knowledge)。
#   约定: 本文件只存"提炼过的知识", 不整段搬运源代码 —— 那是源码区的事。
# ════════════════════════════════════════════════════════════════════════════

CODE_KNOWLEDGE = [
    # ── 语言本质: Python 是什么 ──────────────────────────────────────────
    "Python 是一种解释型、面向对象的高级编程语言，语法简洁，强调可读性，写起来像在读英语句子，靠缩进而不是花括号划分代码块。",
    "Python 是动态类型语言：变量不用先声明类型，赋什么就是什么；解释器一行一行执行，出错的瞬间就在出错那一行停下来。",
    "Python 的底层是 C 语言写的 CPython 解释器，它先把源码编译成字节码，再由虚拟机逐条执行字节码，所以跑起来比 C 编译出来的程序慢一些。",
    "Python 的 GIL 是解释器里的一个全局锁：同一时刻只允许一个线程执行 Python 字节码，所以纯计算用多线程提速有限，要真并行得用多进程。",
    "Python 一切皆对象：数字、字符串、函数、类都是对象，都有类型和属性，所以函数能当参数传来传去，类能动态加方法。",

    # ── 基础语法: 怎么写 ────────────────────────────────────────────────
    "Python 变量直接赋值就能创建，比如 x = 5 就是给 x 这个名字绑定了整数 5；名字只是个标签，对象才真正住在内存里。",
    "Python 的 if 判断写法是 if 条件: 下面缩进写成立时要做的语句，elif 和 else 追加其他分支，条件为真的分支才执行。",
    "Python 的 for 循环用来遍历可迭代对象，写法是 for 元素 in 列表:，循环体缩进写；底层靠迭代器协议，一次取一个元素，不把整个序列搬进内存。",
    "Python 的 while 循环写法是 while 条件:，条件为真就一直循环，直到条件变假或遇到 break；要跳过本次循环用 continue。",
    "Python 用 def 定义函数，写法是 def 函数名(参数):，return 返回结果；不写 return 的函数默认返回 None。",
    "Python 用 class 定义类，构造函数叫 __init__，第一个参数永远是 self，指向实例自己；方法定义在类里，实例调用时自动把 self 传进去。",
    "Python 用 import 导入模块，import math 之后用 math.sqrt 调用；from math import sqrt 可以直接用 sqrt，底层是找到模块文件执行一遍，把里面的名字挂进来。",
    "Python 的 print 把内容输出到屏幕，默认换行；end='' 可以取消换行，sep=',' 可以指定多个值之间的分隔符。",

    # ── 数据结构: 用什么装 ──────────────────────────────────────────────
    "Python 列表 list 用方括号，是有序可变序列，可以增删改，比如 append 加尾、insert 插位、pop 弹出；底层是动态数组，按下标访问是 O(1)。",
    "Python 元组 tuple 用圆括号，是有序不可变序列，创建后不能改，适合存不该变的成组数据；底层和列表一样是数组，但少一层保护。",
    "Python 字典 dict 用花括号存键值对，通过键找值，查找是 O(1)；底层是哈希表，键必须可哈希，所以键不能用列表。",
    "Python 集合 set 用花括号或 set()，存不重复的元素，去重和成员判断都用它；底层也是哈希表，所以元素必须可哈希。",
    "Python 字符串切片用 s[开始:结束:步长]，s[1:5] 取下标 1 到 4，s[::-1] 反转字符串；底层字符串是不可变对象，每次拼接会生成新字符串。",
    "Python 列表推导式把循环和收集压缩成一行，比如 [x*x for x in range(10)] 生成 0 到 9 的平方列表，比手写 for 循环更简洁更快。",
    "Python 的 enumerate 在遍历时同时给出下标和元素，for i, v in enumerate(列表):，不用再自己维护一个计数器。",
    "Python 的 zip 把多个序列按位置配对打包，for a, b in zip(x, y): 同时遍历两个列表，长度不等时以短的为准。",

    # ── 进阶能力: 更厉害怎么写 ─────────────────────────────────────────
    "Python 的 lambda 是单行匿名函数，写法是 lambda 参数: 返回值，适合传给 sorted 的 key 参数这种用完就扔的场景。",
    "Python 装饰器是包装函数的函数：@decorator 写在函数上方，调用时先过装饰器；底层就是函数替换，装饰器接收原函数、返回新函数。",
    "Python 的生成器用 yield 产出值，函数带 yield 就变成生成器，每次 next 执行到下一个 yield；它惰性求值，适合流式处理大文件，不占内存。",
    "Python 闭包是内层函数引用外层函数的变量，外层返回内层，内层记住那个变量；常用在工厂函数和计数器里。",
    "Python 用 try/except 捕获异常，try 里放可能出错的代码，except 后面写异常类型和处理；finally 无论怎样都会执行，适合做清理。",
    "Python 用 with open(路径) as f: 打开文件，with 块结束自动关闭文件；底层是上下文管理器协议，__enter__ 进、__exit__ 出，忘了 close 也不会漏。",
    "Python 多线程用 threading.Thread 起线程，start 开始、join 等待；多进程用 multiprocessing，进程间不共享内存，用 Queue 传数据。",
    "Python 的 async/await 是协程异步：async def 定义协程函数，await 让出控制权，事件循环调度，适合 IO 密集任务，一个线程同时管很多连接。",
    "Python 的 f-string 是格式化字符串的现代写法，f'值是 {x}' 直接把变量嵌进字符串里，比 % 和 format 更直观。",

    # ── 算法与底层原理: 为什么 ─────────────────────────────────────────
    "递归是函数调用自己：必须有递归出口，否则无限调用把栈撑爆；递归适合树、目录、分治这类天然有嵌套结构的问题。",
    "冒泡排序每次把相邻的逆序对交换，一轮把一个最大数沉到底，n 个数最坏 O(n²)；实现简单但慢，小数据量才用它。",
    "二分查找要求序列有序，每次取中间值比较，把搜索区间砍一半，O(log n)；底层原理是每次都确定目标在左半边还是右半边。",
    "链表和列表不同：链表每个节点存数据和下一个节点的指针，插入删除快、按下标找慢；Python 的列表是连续数组，插入头部要整体挪。",
    "哈希表把键通过哈希函数变成桶下标，所以 dict 查找平均 O(1)；冲突时用链地址法或开放寻址解决，所以最坏情况退化到 O(n)。",
    "栈是后进先出，列表的 append 和 pop 就是栈操作，适合括号匹配、函数调用、撤销历史；队列是先进先出，用 collections.deque 实现。",
    "时间复杂度的 O 记法描述算法随数据量增长的耗时：O(1) 与规模无关，O(n) 线性，O(n²) 平方；选算法先看数据量再选复杂度。",
    "Python 的内存管理靠引用计数：对象被引用的次数归零就立刻回收，再配合垃圾回收处理循环引用；所以局部变量用完了内存自动还。",
    "面向对象三大特性：封装把数据和操作绑进类里，继承让子类复用父类，多态让不同对象对同一方法做出不同行为。",
    "Python 参数传递是传对象引用：传不可变对象（数字、字符串）改不动原值，传可变对象（列表、字典）函数里改了外面也变。",
    "函数式编程风格：用 map 对序列每个元素做变换，filter 按条件过滤，reduce 把序列折叠成一个值，配合 lambda 一行写出循环逻辑。",

    # ── 生态与工具: 怎么用 ─────────────────────────────────────────────
    "pip 是 Python 的包管理器，pip install 包名 从 PyPI 下载安装，装错了用 pip uninstall 卸载；第三方库大多装到 site-packages 里。",
    "虚拟环境用 venv 创建，python -m venv 名字 生成独立环境，激活后装的包只属于这个项目，不会污染全局；这是多人项目的标准做法。",
    "numpy 的数组是 Python 列表的提速版：np.array 创建，运算整体广播，底层是连续内存的 C 数组，比 for 循环快几十上百倍。",
    "调试 Python 先 print 中间变量定位，再上 pdb 断点：import pdb; pdb.set_trace() 会在那行停下，能逐行看变量，比瞎猜快得多。",
    "Git 记录代码历史：git add 暂存、git commit 存快照、git push 推远端；每个 commit 是可回退的存档点，写坏代码也不怕。",
    "一个规范的 Python 文件结构：顶部 import、然后是常量、再是函数和类、最后 if __name__ == '__main__': 放入口，这样别人一眼看懂。",
    "写 Python 的原则：变量名要能看出含义，函数只做一件事，先写能跑的再优化；可读性比炫技重要，三个月后读代码的还是你自己。",
]

# 上面的条目由训练循环当「答案」喂给小方 —— 问题侧用代码类提示词,
# 让它学着用这些知识答话; 训练启动时自动追加从自身/历史源码提炼的
# 「函数用法」条目(见 covi1 的 _auto_code_knowledge, 不搬源码原文)。
